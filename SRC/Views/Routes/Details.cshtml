@model TransportManagementSystem.Models.Route

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Details</h1>

<div>
    <h4>Route</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Stop)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Stop)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.RouteNumber">Edit</a> |
    <button type="button" class="btn btn-primary" onclick="location.href = '@Url.Action("Index","Routes")'"><b>Back</b></button>
</div>
